#!/bin/sh

# Set the wallpaper (ensure path is correct)
sleep 1 && feh --bg-scale /usr/share/backgrounds/Fuwafuwa_nanbatto_san_by_amaral-dark.png &

# Add other startup apps here

# Finally, execute dwm
exec dwm